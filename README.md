# Fetch advertisement data from Jobbnorge

## Requirements
- NodeJS (testet with v18.x)

### Install dependencies
`npm run install`

### Compile code
`npm run compile`

### Run application
`node dist/main.js`